---
title: 1.6 最小 psql 生存卡
linkTitle: 1.6 最小 psql 生存卡
weight: 60
type: docs
math: true
breadcrumbs: true
comments: false
book_kind: section
book_chapter: ch01
book_number: '1.6'
book_capability: PG
book_status: scaffold
aliases:
- "/volume-1/postgresql-pigsty-map/06/"
---

> **本节定位**：本节聚焦“最小 psql 生存卡”，在第 1 章的学习路径中承担明确的认知或实践任务。页面先用各目的摘要固定写作范围，后续正文将沿着概念、证据、实践与风险边界逐步展开。

## 1.6.1 用 URI 连接，用 `\l`、`\dn`、`\d` 看对象 {#item-1-6-1}

这一目准备围绕“用 URI 连接，用 \l、\dn、\d 看对象”展开，把这一动作拆成可执行内容，交代输入、操作顺序、预期结果和验收证据。内容将优先使用 PostgreSQL 原生 SQL、系统目录、日志、执行计划或可重复实验作为证据，使它在“最小 psql 生存卡”这一节中承担清晰的认知或实践任务，同时明确它与相邻概念及后续章节的边界。

## 1.6.2 用 `\c` 切库，用 `-c` 与 `-f` 执行 {#item-1-6-2}

这里将围绕“用 \c 切库，用 -c 与 -f 执行”展开，把这一动作拆成可执行内容，交代输入、操作顺序、预期结果和验收证据。内容将优先使用 PostgreSQL 原生 SQL、系统目录、日志、执行计划或可重复实验作为证据，使它在“最小 psql 生存卡”这一节中承担清晰的认知或实践任务，同时明确它与相邻概念及后续章节的边界。

## 1.6.3 用 `\o` 或 `-A -t` 保存输出 {#item-1-6-3}

本目计划围绕“用 \o 或 -A -t 保存输出”展开，把这一动作拆成可执行内容，交代输入、操作顺序、预期结果和验收证据。内容将优先使用 PostgreSQL 原生 SQL、系统目录、日志、执行计划或可重复实验作为证据，使它在“最小 psql 生存卡”这一节中承担清晰的认知或实践任务，同时明确它与相邻概念及后续章节的边界。

## 1.6.4 用 `\q`、`Ctrl-C` 安全退出与中断 {#item-1-6-4}

本目将围绕“用 \q、Ctrl-C 安全退出与中断”展开，把这一动作拆成可执行内容，交代输入、操作顺序、预期结果和验收证据。内容将优先使用 PostgreSQL 原生 SQL、系统目录、日志、执行计划或可重复实验作为证据，使它在“最小 psql 生存卡”这一节中承担清晰的认知或实践任务，并指出正文必须保留的反例、风险提示与验收点。

---

[返回本章目录](../) · [查看全书目录](/toc/) · [查看索引中心](/indexes/)
