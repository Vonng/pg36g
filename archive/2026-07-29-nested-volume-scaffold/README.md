# 两卷嵌套版脚手架归档

本目录封存 2026-07-29 生成的第一版完整内容脚手架。该版本把 `volume-1/` 与 `volume-2/` 作为 Hugo 内容与导航层级，共包含 302 个文件。

随后采用扁平章节导航：36 个语义章节目录直接位于 `docs/` 顶层，“上卷／下卷”只保留为章节元数据中的概念标签。旧版嵌套 URL 由新页面的 Hugo aliases 继续兼容。

这里归档的是生成内容，不是最初的手写正文。最初 76 个内容文件及图片（另有 2 个未纳入 Git 的 Finder 元数据文件）的权威快照仍位于：

```text
archive/2026-07-29-pre-scaffold/docs/
```

若要比较两种导航结构，可读取本目录下的 `docs/`；不要直接把它覆盖回当前 `docs/`。
