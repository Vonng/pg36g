---
title: "与前后章衔接 + 自检与修正"
weight: 310
math: true
breadcrumbs: false
---

## 承接 ch01

- 直接复用 ch01 已搭建的 Linux + Pigsty + PostgreSQL 沙箱。
- 默认读者具备基础连接能力，本章不重复安装部署流程。
- 本章把“能连接”升级为“能定位、能验证、能回滚、能授权”。

## 交付给 ch03

ch03（`psql` 与工具链）会直接复用本章交付的 6 类资产：

- 连接快照命令（`\\conninfo` + 会话信息 SQL）
- 对象层级查询模板（库/模式/表）
- 主线对象 SQL（`app.task`）
- 事务回滚演练脚本
- 系统目录查询模板
- 权限与扩展验证脚本

## 自检清单（8 项）

1. 是否只覆盖 ch02 范围，没有越界到工具大全或运维专题。
2. 是否保持章节标题与编号不变。
3. 是否明确承接 ch01，且为 ch03 做了可复用输入。
4. 核心节数是否为 7 节。
5. 每节是否至少包含 1 个可执行动作。
6. 是否只保留 `todo_app` 主线 + `reporter` 支线。
7. 验收标准是否可量化（成功/失败、行数、是否存在等）。
8. 首次引入术语是否控制在预算内。

## 本轮主动修正（3 处）

1. 将“摘要占位”全部替换为“动作驱动正文”，避免空骨架。
2. 为每节补充可验证结果，减少“看懂但不会做”的断层。
3. 增加参考资料入口页，统一沉淀老彭文章、官方文档、社区文献。
